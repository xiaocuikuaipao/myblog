spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    username: root
    password: root


logging:
  level:
    root: info
    com.usst: debug
  file:
    path: log/blog.log

#mybatis-plus:
#  mapper-locations: classpath:mapper/**/*.xml